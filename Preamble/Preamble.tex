\documentclass[twoside,openright,10pt] {book}

\usepackage[draft]{showkeys}

% Setup sprog og orddeling
\usepackage[danish]{babel}
\usepackage[applemac]{inputenc}
\usepackage[T1]{fontenc}

% Setup Math
\usepackage{amsmath, amsthm, amssymb, amsfonts}
\usepackage{mflogo}

% Allow you to manipulate the page a bit
\usepackage[margin=1.5cm, a5paper]{geometry}

% Pulls the page out a bit - makes it look better
%\usepackage{a5wide}

% Remove paragraph indentation
\usepackage{parskip}

%Include graphics and drawing
\usepackage{graphicx,tikz,SIunits}


% Ptovide nice looking tables
\usepackage{booktabs}

%Allow you to rotate figures and tables
\usepackage{rotating}

% Allow shading of table cells
\usepackage{colortbl}
%Define a simple command
\newcommand{\gray}{\rowcolor[gray]{.9}}

\usepackage{textcomp}

%Provide commands to make subfigures
\usepackage{subfigure}
\usepackage{wrapfig}

%Typeset URLs sensibly
\usepackage{url}

%Makes references hyperlinks in PDF
\usepackage[colorlinks]{hyperref}

% Provide Harvard-style referencing
\usepackage{natbib}
\bibpunct{(}{)}{;}{a}{,}{,}

% Provide good access to colours
\usepackage{color}
\usepackage{xcolor}

%Simple command to insert TODO's
\newcommand{\todo}[1] {\textbf{\textcolor{red}{#1}}}

% Allows fancy stuff in the page header
\usepackage{fancyhdr}
\pagestyle{fancy}

\definecolor{MGB}{HTML}{080a56}
\definecolor{MGR}{HTML}{e0231b}

%Define fonts
\usepackage{fontspec}
\setmainfont[Ligatures=TeX]{Arial}
%\setromanfont[Ligatures=TeX]{Neo Tech Std}
%vastly improves the standard formatiing
\usepackage[margin=10pt, font=small, labelfont=bf, labelsep=endash]{caption}

\usepackage{makeidx}
\makeindex

%Standard titlel og forfatter
\title{\fontspec{Neo Tech Std}\color{MGR}\MakeUppercase{Einsteins Relativitetsteori}}
\author{\color{MGB}Thomas Mellergaard Amby\\ {\small\color{MGB}- Marselisborg Gymnasium - }}
\date{2014}

\graphicspath{{Figures/}}
% ------ Macros ----- %
\newcommand{\currentchapter}{}
\let\oldchapter\chapter
\renewcommand{\chapter}[1]{\oldchapter{\fontspec{Neo Tech Std Light}#1}\renewcommand{\currentchapter}{#1}}


\newcommand{\currentsection}{}
\let\oldsection\section
\renewcommand{\section}[1]{\oldsection{#1}\renewcommand{\currentsection}{#1}}

\newcommand{\currentsubsection}{}
\let\oldsubsection\subsection
\renewcommand{\subsection}[1]{\oldsubsection{#1}\renewcommand{\currentsubsection}{#1}}

\newcommand{\currentsubsubsection}{}
\let\oldsubsubsection\subsubsection
\renewcommand{\subsubsection}[1]{\oldsubsubsection{#1}\renewcommand{\currentsubsubsection}{#1}}

% Put the text in r-header and l-header
\fancyhead{}
\fancyhead[LE]{\fontspec[SizeFeatures={Size=8pt}]{Neo Tech Std}\currentchapter}
\fancyhead[RO]{\fontspec[SizeFeatures={Size=8pt}]{Neo Tech Std}Version 1.0}
\fancyhead[RE,LO]{\fontspec[SizeFeatures={Size=8pt}]{Neo Tech Std}2014 \copyright ~T.M.~Amby}
\fancyfoot{}
\fancyfoot[LE,RO]{\fontspec[SizeFeatures={Size=8pt}]{Neo Tech Std}\thepage}
\fancyfoot[LO,RE]{\fontspec[SizeFeatures={Size=8pt}]{Neo Tech Std}Marselisbog Gymnasium}
\renewcommand{\headrulewidth}{.4pt}
\renewcommand{\footrulewidth}{.4pt}

%% CHAPTER STYLE 1
\usepackage[explicit]{titlesec}
\usepackage{minitoc}
\usepackage{epigraph}
%\usepackage{xpatch}
\usepackage{lmodern}
%
\newlength\ChapWd
\settowidth\ChapWd{\huge\chaptertitlename}
%
\titleformat{\chapter}[display]
	{\normalfont\filventer\sffamily}
	{\tikz[remember picture, overlay]
		{
			\node[fill=MGB, font=\fontsize{60}{72}\selectfont\color{white},anchor=north east, minimum size=\ChapWd]
			at ([xshift=-15pt,yshift=-15pt]current page.north east)
			(numb) {\thechapter};
			\node[rotate=90, anchor=south, inner sep=0pt, font=\Huge] at (numb.west) {\chaptertitlename};
		}
	}{0pt}{\fontsize{33}{40}\selectfont\color{MGR}#1}[\vskip1pt\centering\Large***]
	\titlespacing*{\chapter}
	{0pt}{50pt}{10pt}
%	
%
%\makeatletter
%\xpatchcmd{\ttl@printlist}{\endgroup}{{\noindent\color{MGB}\rule{\textwidth}{1.5pt}}\vskip30pt\endgroup}{}{}
%\makeatother

%\newcommand\DoPToC{%
%\startcontents[chapters]
%\printcontents[chapters]{}{1}{\noindent{\color{MGB}\rule{\textwidth}{1.5pt}}\par\medskip}%
%}

%\setlength\epigraphrule{0pt}
%\renewcommand\textflush{flushright}
%\renewcommand\epigraphsize{\normalsize\itshape}	

%Style the chapter
%\usepackage{graphics}
%\titleformat{\chapter}[display]
%	{\normalfont\Large\raggedleft}
%	{\color{MGR}\MakeUppercase{\chaptertitlename}%
%		\rlap{ \resizebox{!}{1cm}{\thechapter}  \rule{2cm}{1cm}}}
%		{1pt}{\Huge}
%		
%\titlespacing*{\chapter}{0pt}{30pt}{20pt}
	
%\renewcommand{\eqref}[1]{\eqref{eq:#1}}

\usepackage{lipsum}
